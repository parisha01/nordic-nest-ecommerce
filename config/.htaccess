# Blocks direct browser access to this folder (DB credentials).
# Being INSIDE config/ means no <Directory> wrapper is needed here.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
